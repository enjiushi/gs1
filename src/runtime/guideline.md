# src/runtime/

Core runtime services that drive fixed-step execution, queue dispatch, clocks, and host integration.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `engine_feedback_buffer.h`: Buffer/storage for translated engine feedback events.
- `engine_message_queue.h`: Queue helpers for engine-originated messages entering gameplay.
- `fixed_step_runner.h`: Fixed-timestep runner used by the runtime update loop.
- `game_runtime.h`: Main runtime type declarations and update entry points, now versioned for a runtime-create contract that accepts both the host-supplied shared project-config root and an optional adapter-config payload.
- `game_runtime.cpp`: Runtime loop implementation, integration wiring, startup resolution of the shared project-config root into the shared `project/content/` TOML set consumed by gameplay while still accepting an adapter-supplied initialization config blob that can carry that root path, projected engine/HUD message emission, tile-upsert coalescing against the last emitted visible tile snapshot including fine plant-density buckets for withering hints plus quantized local-wind, stable normalized device-integrity bucket projection, and protection-channel projection for plant-reactive viewers and protection heatmaps, sparse plant/device visual upsert-remove emission keyed by gameplay-owned anchor IDs for engine-side instanced renderers with authored footprint, height, and trait hints so adapters do not infer presentation categories locally, active site-modifier list projection with timed-buff remaining-game-hour buckets plus manual end-modifier UI action translation, temporary Site 1 starter-tile projection probe logs for sent-versus-suppressed tile upserts now marked as debug-level so activity-only smoke logging suppresses them, authoritative phone home/app-panel state plus tracked phone-open visibility and phone listing/task-count projection emission, tracked worker-pack and device-storage inventory-view projection with stable already-open device snapshots while the worker-pack panel toggles, site-panel exclusivity coordination between inventory, phone, and tech-tree while now allowing the Alt-triggered protection selector/overlay to remain active during build/plant placement preview, dedicated hypothetical placement-impact overlay tile projection for shelter-and-number hover previews without mutating committed gameplay state, pending-claim plus claimed task-list projection, HUD meter emission for health, hydration, nourishment, energy, and morale, a reward-claim-resolved path that flushes updated site state before emitting a host-facing celebration cue, crafted-item completion handling that now reopens the authoritative output storage and emits a dedicated crafted-output cue for viewer-side slot highlighting, campaign-unlock delta tracking that now emits the same celebration surface for newly unlocked reputation unlockables and faction-tech nodes, site-result `OK` return action projection, regional-map support previews derived from authored modifier totals plus tile-position projection, revealed-frontier-only regional-map snapshot emission, and now both a panel-slot/list UI projection path for authored Godot main-menu and regional-map shells plus a typed progression-view projection for the research/unlock ladder so gameplay publishes semantic unlock/tech entry data without telling adapters how to lay it out, while the remaining unmigrated panels still use the older semantic UI-setup batches, alongside player-facing site-session cash projections derived from internal cash points for HUD/phone/status labels, reputation-scaled effect-parameter readouts, and while treating batched ecology task-tracking summaries as internal gameplay traffic rather than direct projection triggers.
- `id_allocator.h`: Lightweight runtime ID allocation helpers.
- `message_queue.h`: Internal gameplay message queue declarations.
- `random_service.h`: Runtime random-number service/state wrapper.
- `runtime_clock.h`: Canonical runtime day-length constants plus real-seconds-to-in-game-minutes conversion helpers.
