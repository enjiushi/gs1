# src/site/systems/

Site-owned systems that subscribe to messages/events and mutate only the state they own.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `action_execution_system.h`: Action execution system interface.
- `action_execution_system.cpp`: Executes queued site actions using the canonical real-seconds-to-action-minutes conversion, derives action duration plus deferred worker action costs from the previous-frame work-efficiency snapshot, scales authored worker meter costs from the worker tile's current local weather when the action starts including morale, applies active resolved action-cost buff modifiers before storing those costs, applies the stored costs when the action completes, reads recipe-authored craft duration and worker meter costs, now requiring a nearby hammer before device/tool craft actions can start while repairs still require a carried hammer, emits follow-up gameplay messages, supports harvest actions for density-ready plants using table-driven harvest rows whose exact multi-item results are resolved and locked at action start, now always awarding exactly one dedicated main harvest item when the plant is above the harvest density threshold while leaving dry-grass and seed drops on their independent authored side-roll rules and layering an additive global-plus-Bureau harvest-bonus bundle roll on top, seeds item-based plant placements at full `100` density on completion, applies Village shovel carry bonuses to Plant/Excavate duration and costs from resolved modifier-owned Village effect state, uses depth-specific rough/careful/thorough excavation durations alongside deterministic depth-specific tier-biased stone loot plus Village depth-unlock and loot-rebalance hooks, with careful and thorough depth access now arriving on spaced Village excavation-income beats, and re-arms plant placement mode after successful item-based planting when matching seeds remain in the worker pack.
- `camp_durability_system.h`: Camp durability system interface.
- `camp_durability_system.cpp`: Applies camp wear/failure effects to camp durability state using content-authored wear-rate and service-threshold tuning.
- `craft_system.h`: Craft system interface.
- `craft_system.cpp`: Handles crafting progress, completion, and crafting-related state updates, including on-demand craft-context option projection that now filters out hammer-gated device/tool recipes when no nearby hammer is available, plus idle-gated cached nearby-item refreshes for crafting stations while consuming the full-width nearby-item entity cache so newly delivered items do not disappear through 32-bit id truncation.
- `device_maintenance_system.h`: Device maintenance system interface.
- `device_maintenance_system.cpp`: Applies repair/maintenance behavior for site devices and emits device-condition change messages when integrity or occupancy changes, including startup snapshots for task-tracking subscribers, now using sparse device-entity iteration instead of dense full-tile scans for per-frame wear updates, scaling weather wear from the normalized max of heat/wind/dust against authored per-structure max-weather loss speeds while still skipping wear on fixed-integrity special devices such as the delivery crate.
- `device_support_system.h`: Device support system interface.
- `device_support_system.cpp`: Resolves support effects and dependencies for site devices using content-authored evaporation tuning, now by iterating sparse device entities instead of scanning the full tile grid.
- `device_weather_contribution_system.h`: Device weather contribution system interface.
- `device_weather_contribution_system.cpp`: Retains owner-specific per-tile device weather contributions across frames, invalidates dirty target tiles from device-state messages plus snapped 8-direction wind changes, and recomputes only the affected tiles by scanning nearby device anchors.
- `ecology_system.h`: Ecology system interface.
- `ecology_system.cpp`: Updates plants, burial, watering, harvest-density setbacks, broader ecology progression, and highway-target sand-cover accumulation for objective-driven site progress, including verbose plant-density change logs, temporary Site 1 starter-patch density probe logs now tagged as debug-level present logs, sparse active-ecology ECS iteration for occupied tiles, event-driven dirty handling for non-occupied/special-case updates, a separate highway/global pass, batched ecology summary messages for downstream consumers, the simplified growth-pressure computation that compares local weather and effective resistance on the same authored scale while keeping non-growable setup plants at zero weather pressure, a single linear density-change formula centered on `growthPressure = 50`, a temporary Site 1 onboarding guard that suppresses negative density deltas on the authored starter Desert Ephedra tiles while the stable-starter onboarding step remains unclaimed, and owner-emitted living-plant stability messages for task tracking.
- `economy_phone_system.h`: Economy phone system interface.
- `economy_phone_system.cpp`: Handles phone storefront interactions using content-authored seeded listings plus generated buy-stock CP budgets and delivery settings, per-site-session transient cash tracked internally in cash points, item buy/sell listing prices derived from resolved item internal cash points plus shared buy/sell multipliers, authored-stock-driven availability where survival/basic-material entries stay explicit and plant-seed entries appear only when their linked plants are unlocked, periodic rerolls of limited buy stock outside onboarding lockout, immediate delivery-crate routing for bought items, owner-confirmed buy/sell completion messages for onboarding-task progress, task-reward money/unlockable reveal awards routed through message handling, and the shared full-width phone inventory cache consumed for global quantity checks.
- `failure_recovery_system.h`: Failure recovery system interface.
- `failure_recovery_system.cpp`: Resolves failure fallout and recovery flow for the site.
- `inventory_system.h`: Inventory system interface.
- `inventory_system.cpp`: Applies item moves, uses, inventory ownership changes, explicit item-submission consumption for task hand-ins, site-start loadout seeding into the delivery crate, harvested-output insertion into the worker pack, immediate delivery-crate insertion with queued overflow retries, tracked worker-pack/device-storage panel open-close requests, owner-confirmed transfer/submit/use/craft completion messages for onboarding-task progress, hammer-gated craft-commit validation for device/tool outputs, inventory-confirmed consumable completion messages for drink/eat/medicine follow-up systems, and sparse device-entity iteration when ensuring device-backed storage containers.
- `local_weather_resolve_system.h`: Local weather resolve system interface.
- `local_weather_resolve_system.cpp`: Resolves final local weather each frame from base weather plus retained owner-specific plant/device contribution channels, now by iterating only the narrow tile ECS components it actually reads or owns instead of fan-out full-tile reads, marks projected plant tiles dirty when visible wind changes and now also dirties tiles when support-only shelter values change so protection overlays stay in sync on nearby empty tiles, emits startup plus live tile state-change messages for task tracking, and currently includes temporary Site 1 starter-patch local-weather probe logs tagged at debug level for verbose-only smoke output.
- `modifier_system.h`: Modifier system interface.
- `modifier_system.cpp`: Applies and expires site modifiers/effects using shared content-authored modifier definitions, stores permanent run modifiers and timed item buffs in one shared active site-modifier list keyed by `modifier_id`, activates item-granted modifiers from inventory-confirmed consumable completion including Village custom-food modifier routing plus split linear-tech buff-effect scaling, overwrites any existing active entry with the same `modifier_id`, supports manual timed-modifier end requests, caps concurrent distinct timed buffs from configurable gameplay tuning plus resolved timed-buff-cap bias, ticks timed durations on in-game site time while only dirtying projection when the displayed remaining-game-hour bucket changes or the modifier set changes, and resolves terrain factor weights/biases, worker action-cost modifier totals, harvest-output cash-point multiplier totals, plus shared numeric Village-tech and Bureau harvest-bonus effect snapshots consumed by other owner systems, with Village excavation-support, sustain, food, and buff beats redistributed across the ladder instead of clustering the excavation ladder into one band.
- `phone_panel_system.h`: Phone panel system interface.
- `phone_panel_system.cpp`: Owns phone home/app-panel section state plus tracked phone open-close visibility and authoritative projected phone listings/task counts for the adapter, including pending-claim and claimed-history counts plus derived sell-list pricing from internal cash-point item values, while intentionally filtering frozen contractor offers out of the current player-facing phone projection and consuming the shared full-width phone inventory cache for quantity-based presentation.
- `plant_weather_contribution_system.h`: Plant weather contribution system interface.
- `plant_weather_contribution_system.cpp`: Retains owner-specific per-tile plant weather contributions across frames, invalidates dirty target tiles from both single-tile and batched ecology messages plus snapped 8-direction wind changes, and recomputes only the affected tiles by scanning nearby plant/ground-cover sources, now deriving plant heat/wind/dust shelter from resistance/tolerance values for outward tiles only while leaving a plant's own footprint to rely on self resistance instead of self-fed local-weather shielding, scaling authored aura and wind ranges by plant footprint multiples, applying authored nearby-protection ratios before writing outward shelter to neighboring tiles, and deriving plant-side salinity rehabilitation from `saltTolerance` only for support-focused plants.
- `placement_validation_system.h`: Placement validation system interface.
- `placement_validation_system.cpp`: Validates structure placement requests against tile/world constraints.
- `site_completion_system.h`: Site completion system interface.
- `site_completion_system.cpp`: Determines site success/failure outcomes for the active site objective mode, including pure-survival time completion, cash-target survival goal completion from site money, and green-wall hold/timer-pause evaluation.
- `site_flow_system.h`: Site flow system interface.
- `site_flow_system.cpp`: Coordinates major site flow transitions during a run, including worker motion and approach/interaction movement handling.
- `site_time_system.h`: Site time system interface.
- `site_time_system.cpp`: Owns fixed-step-derived site clock, day index, and day-phase advancement, including the periodic site-refresh tick that drives normal task-board rerolls and phone buy-stock restocks on the shared authored cadence.
- `site_system_context.h`: Shared dependency bundle passed into site systems, including owner-scoped tile component read/write helpers used to keep systems within their declared ECS ownership boundaries.
- `task_board_system.h`: Task board system interface.
- `task_board_system.cpp`: Manages board listings, acceptance, task-instance resolution from single-value template fields with `0`/`None` procedural sentinels, per-task cash-point difficulty and reward-budget valuation from authored task timing/risk plus resolved item/structure choices and player-meter-derived action costs, site-keyed onboarding-task seeding on site start with the Site 1 chain now surfacing an explicit hammer-craft step before hammer-gated device/tool crafting, one-at-a-time onboarding follow-up surfacing with auto-accepted next steps, generated non-onboarding board refreshes on the shared periodic refresh tick once onboarding clears, buy-task candidate selection aligned to the authored phone stock pool plus unlocked plant seeds, task-owned mirror caches fed only by owner-emitted buy/sell/transfer/submit/plant/craft/build and worker/tile/device/living-plant-stability state messages plus batched ecology tile summaries, earn-money progress and money rewards now tracked in internal cash points, explicit pending-claim task state before history, bundled onboarding reward claims that can chain multiple delivery/money/reputation candidate effects while defaulting to the first draft option when the UI does not project a specific candidate id, a queued task-reward-claim-resolved follow-up emitted after reward effects are queued so runtime/hosts can celebrate on the authoritative resolved state, duration accumulation over those subscribed conditions, message-driven living-plant non-withering checks, and site-start board reset plus tracking-cache initialization.
- `weather_event_system.h`: Weather event system interface.
- `weather_event_system.cpp`: Applies site-owned weather/event state changes, including authored default-weather baselines, temporary Site 1 startup weather probe logs tagged at debug level for verbose-only smoke output, start/peak/end timeline interpolation for additive event pressure that smooths back to baseline weather, and recurring one-sided harsh-weather waves for both highway-protection and cash-target survival objectives.
- `worker_condition_system.h`: Worker condition system interface.
- `worker_condition_system.cpp`: Resolves worker condition from previous-frame state plus accumulated frame deltas, recomputes derived energy-cap and work-efficiency snapshots, applies content-authored passive environment-driven meter loss with shelter softening across the requested health, hydration, and nourishment heat-wind-dust ratios, applies Village weather-loss mitigation and custom recovery-food passives from resolved modifier-owned Village effect state, resolves hydration/nourishment-scaled background energy recovery except while a site action is actively executing, applies max-weather-driven morale recovery/decline plus resolved morale-support speed, consumes both direct worker-meter delta messages and inventory-confirmed item-use completion messages for consumable meter gains including Village custom-food cash-point splits, and emits startup worker-meter snapshots for task tracking.
