# src/content/

Prototype content storage, indexing, validation, and TOML-backed content loading used to feed gameplay definitions.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `content_database.h`: Aggregate content database types that hold loaded definitions, including the shared gameplay-tuning slice consumed by multiple runtime systems, a single shared modifier-definition table keyed by `modifier_id` for nearby auras, permanent run modifiers, and timed consumable modifiers, excavation depth/tier loot authoring, plus site-keyed onboarding task seed records.
- `content_index.h`: Lookup/index helpers over content definitions, including the shared modifier-id index resolved from authored content references.
- `content_loader.h`: Content loading entry points and loader-facing contracts.
<<<<<<< HEAD
- `content_loader.cpp`: TOML parsing and startup loading for the externalized prototype content slices, including the content-backed campaign/site graph, authored regional-map tile coordinates, campaign setup, site phone-listing seeds, authored site default-weather and starter-plant bootstrap data, shared gameplay tuning tables for worker condition, player-meter cash-point valuation, item buy/sell price multipliers, ecology, modifier, device-support, and camp-durability systems including the simplified resistance-vs-weather growth-pressure scaling, single-value task-template metadata with `0`/`None` procedural sentinels plus authored expected in-game hours and risk multipliers, site-keyed onboarding task seed overrides with optional fixed reward candidates, fallback item internal cash-point values plus meter-derived consumable valuation used by task/reward scoring, campaign cash, reward money, contractor prices, and delivery fees normalized into internal cash points, excavation depth/tier loot tables with depth-specific within-tier item splits plus excavation-only merchandise items, site action tuning with per-action worker nourishment and morale cost plus authored weather-to-action-cost coefficients and recipe-authored craft duration and worker meter costs, per-plant harvest-output metadata plus authored nearby-protection ratios, unified modifier definitions keyed by `modifier_id` with optional authored display text, `0`-duration permanent entries, timed consumable durations, optional `timed_buff_cap_bias`, and action-cost modifier deltas, plus modifier-system gameplay tuning for the base active-buff cap, faction tech progression authoring with per-node base/enhancement choice metadata, lighter `1-8` base-tech reputation bands, higher `8-15` enhancement reputation bands, internal cash-point values, reputation-scaled effect parameters, and optional item/plant/structure/recipe grant payloads, direct-purchase unlockable phone listings with internal cash-point values converted to player-facing cash, per-plant reputation unlock thresholds, and multi-channel structure support values used by resolved tile computation.
- `content_loader.cpp`: TOML parsing and startup loading for the externalized prototype content slices, including the content-backed campaign/site graph, authored regional-map tile coordinates, campaign setup, site phone-listing seeds, authored site default-weather and starter-plant bootstrap data, shared gameplay tuning tables for worker condition, player-meter cash-point valuation, item buy/sell price multipliers, ecology, modifier, device-support, and camp-durability systems including the simplified resistance-vs-weather growth-pressure scaling, single-value task-template metadata with `0`/`None` procedural sentinels plus authored expected in-game hours and risk multipliers, site-keyed onboarding task seed overrides with optional fixed reward candidates, fallback item internal cash-point values plus meter-derived consumable valuation used by task/reward scoring, campaign cash, reward money, contractor prices, and delivery fees normalized into internal cash points, excavation depth/tier loot tables with depth-specific dig duration and within-tier item splits plus excavation-only merchandise items, site action tuning with per-action worker nourishment and morale cost plus authored weather-to-action-cost coefficients and recipe-authored craft duration and worker meter costs, per-plant harvest-output metadata plus authored nearby-protection ratios, unified modifier definitions keyed by `modifier_id` with optional authored display text, `0`-duration permanent entries, timed consumable durations, optional `timed_buff_cap_bias`, and action-cost modifier deltas, plus modifier-system gameplay tuning for the base active-buff cap, faction tech progression authoring with per-node base/enhancement choice metadata, lighter `1-8` base-tech reputation bands, higher `8-15` enhancement reputation bands, internal cash-point values, reputation-scaled effect parameters, and optional item/plant/structure/recipe grant payloads, direct-purchase unlockable phone listings with internal cash-point values converted to player-facing cash, per-plant reputation unlock thresholds, and multi-channel structure support values used by resolved tile computation.
- `content_lookup.cpp`: Runtime-backed definition accessors used by gameplay systems after content is loaded, including the shared gameplay-tuning lookup plus resolved item internal cash-point, derived buy-price, derived sell-price helpers, shared modifier lookup by `modifier_id`, and nearby-aura versus run-modifier total resolution used by the site modifier system.
- `content_validator.h`: Validation entry points for loaded content data.
- `content_validator.cpp`: Validation checks for loaded authored content, campaign/site graph/setup references, unique regional-map site tiles, site default-weather and starter-plant bootstrap values, shared gameplay-tuning ranges including worker background energy-recovery fields plus positive player-meter cash-point valuation and positive item buy/sell multipliers, plant resistance/tolerance ranges plus nearby-protection ratios for the simplified shelter model, seeded phone listings, single-value task-template references plus non-negative task timing/risk inputs, site-keyed onboarding task seed references, resolved item internal cash-point coverage with meter-derived consumables omitting fallback pricing and non-meter items requiring explicit internal pricing, shared modifier-id coverage for site nearby auras, item-granted timed modifiers, run-modifier rewards, assistant packages, and technology-linked modifiers, modifier duration/action-cost ranges, excavation-only merchandise rules plus depth+tier excavation-table totals, tech and unlockable-listing internal cash-point alignment, site action coverage plus non-negative worker meter costs and weather-to-action-cost coefficients, harvest-item coherence, faction tech progression references plus the lighter `1-8` base-tech band and overlapping `8-15` enhancement band validation, optional tech grant payload validation, per-plant reputation unlock thresholds, and cross-file references.
- `defs/`: Per-domain content definition structs, including excavation depth/tier loot definition records and the shared gameplay-tuning definition used by multiple gameplay systems.
- `tables/`: External TOML authoring files for the prototype content slices, including the site/campaign graph content, seeded phone listings, harvest-output and excavation-merchandise item rows, unified modifier rows, site-action tuning values, and the shared gameplay-tuning table for cross-system numeric factors.
- `prototype_content.h`: Prototype content package declarations, including authored site objective modes/config, regional-map tile coordinates, site default-weather and starter-plant bootstrap data, campaign setup/loadout data, regional-support seeds, site faction ownership, site-completion rewards, and seeded phone listings with direct-purchase unlockable internal cash-point valuation.
- `prototype_content.cpp`: Table-backed prototype campaign/site accessors used by campaign/site factories.
