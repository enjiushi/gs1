# tests/runtime/

Runtime-level tests and probes that validate queue flow, projections, and performance characteristics.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `game_runtime_message_chain_test.cpp`: Verifies chained gameplay message dispatch behavior and projected task-completion flow in the runtime, including explicit test-side task seeding after the production Site 1 bootstrap now auto-surfaces and auto-accepts the first authored onboarding task, pending-claim-to-claimed reward-claim projection plus host cue emission from both direct gameplay messages and the host-submitted `CLAIM_TASK_REWARD` UI action path, crafted-item completion reopening the output storage plus emitting the dedicated crafted-output cue, HUD nourishment-meter transport, player-facing site-session cash plus campaign-reputation projection from internal cash-point economy state, and newly unlocked shared-reputation/faction-tech campaign content now emitting the same host-side celebration cue path used by reward claims.
- `godot_debug_http_protocol_test.cpp`: Focused Godot-adapter contract regression covering the optional loopback debug HTTP control parser plus the tracked regional-map scene loadout-panel node contract, verifying semantic UI/site/inventory/movement payload translation, rejection of invalid action names, and that the regional selection shell keeps its inventory-style loadout grid nodes instead of regressing to the old text-summary node.
- `godot_director_scene_policy_test.cpp`: Focused pure C++ regression covering the Godot director's scene-switch policy so deploy-side app-state changes that stay within the same screen family reuse the active scene instead of rebuilding it and risking duplicate exclusive snapshot subscribers, while also pinning the Godot adapter rule that only the snapshot-opening `BEGIN_*_SNAPSHOT` messages remain single-subscriber.
- `godot_regional_map_click_policy_test.cpp`: Focused pure C++ regression covering the regional-map world-click policy so blank-map clicks stay consumed, only issue clear-selection when something was selected, and never regress into leaking the click to unrelated UI.
- `game_runtime_performance_test.cpp`: Runtime performance-oriented test coverage with deterministic mixed-site seeding that now adds 20 authored plant placements across multiple plant types on top of the bootstrapped site.
- `game_runtime_projection_test.cpp`: Verifies projected runtime/UI state output, including regional-map support projection, authored regional-map tile positions and derived links, the full four-site prototype regional-map snapshot with locked-site state preserved for greyed markers, typed progression-view projection for the research/unlock ladder from both the regional map and active site sessions with compact per-entry unlock and tech payloads, campaign-resource projection for total plus Village/Forestry/University reputation values, reputation-scaled effect-parameter readouts, single-crate site bootstrap plus starter Desert Ephedra patch projection with the current base Site 1 `8`-slot worker-pack expectation unless Village pack techs are unlocked, regional loadout list-item projection, mixed accepted/pending-claim task projection with preserved per-task progress values, tile-upsert delta coalescing for fine visible plant-density buckets, full-`100` density projection for item-based plant placements, local-wind, protection channels, stable occupied-device integrity bucketing, excavation-depth visibility plus ecology-driven terrain-appearance changes, sparse plant/device visual payload hints used by native engine instancers, weather-event timeline projection, tracked worker-pack/device-storage inventory-view presentation including the crate-open then worker-pack-open regression, neutral inventory-slot-tap host input for storage transfer and item use, authoritative phone home/app-panel state emission with task counts, open-close visibility, unread-badge flags, and typed UI-surface visibility messages for the engine-owned inventory/phone/overlay/tech shells, player-facing site-session cash projection from internal cash-point economy state, the Alt-triggered site protection selector plus overlay-mode projection including the occupied-only plant-density/device-integrity option, the authored Site 1 onboarding task projection on bootstrap, inventory-phone-tech panel exclusivity behavior, phone listing add/remove projection behavior including direct-buy stock depletion on the purchased listing instead of relying on list order, phone section-change updates, and runtime delivery pacing under the dedicated time-system architecture, now asserting directly on authoritative engine-message output instead of rebuilding a shared projection-cache model in the test host.
- `local_weather_perf_probe.cpp`: Focused performance probe for local weather processing, including per-stage plant/device/resolve timing breakdowns for dense-cover debug investigation plus deterministic mixed authored plant placements so the plant-contribution stage runs against a representative planted site.
- `smoke_engine_host_threading_test.cpp`: Verifies the smoke host's queue-versus-published-snapshot split, including frame-thread draining of queued commands, coalesced site-move input submission, timed-modifier remaining-game-hour patch publication, HUD nourishment-meter patch serialization, campaign-resource patch serialization for total plus per-faction reputation values, the frame-timing breakdown that separates smoke-host work from gameplay-DLL API time, and smoke-viewer asset contract regressions that keep the regional-map cash/task/modifier panels truly collapsed, preserve direct left-click stack-tap behavior for carried inventory items, keep the site-active tech-tree control on its own DOM anchor instead of coupling it to the player-pack panel, retain the nourishment player-meter bar in the viewer asset, keep nested tile-context submenus vertically anchored to the triggering parent row instead of resetting to the parent panel top, and preserve the responsive visual-viewport plus compact-laptop and short-height layout hooks used by the browser viewer.
- `timed_modifier_projection_test.cpp`: Focused runtime regression for timed buffs that verifies the site-modifier projection transports remaining game hours and only republishes when the displayed hour bucket changes.
- `site_system_message_flow_test.cpp`: Verifies message flow between site systems through the runtime path using prototype campaign state plus dedicated campaign/site time systems.
