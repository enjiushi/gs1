# tests/smoke/

DLL-loading smoke harness, scripted smoke runner, live-state server, and visual smoke viewer support.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `assets/`: Static assets used by the visual smoke experience.
- `scripts/`: Scripted smoke scenario files.
- `smoke_engine_host.h`: Smoke host abstraction over the gameplay DLL runtime, including the thread-safe incoming command queue, neutral site inventory slot-tap queueing, the shared host-side runtime ABI loader from `src/host/`, the published immutable live-state snapshot used by the visual smoke host, projected active site-modifier entries with timed-buff remaining-game-hour buckets for the viewer HUD, recent one-shot cue projection buffering for browser-side audio playback plus reward-claim, campaign-unlock, and crafted-output celebration surfaces, site-protection overlay snapshot fields, and dedicated placement-preview impact-tile overlay payloads for hypothetical shelter-and-number hover previews, with phone/hud/campaign money projections stored as player-facing cash floats, phone-panel unread-badge flags carried through the site snapshot, HUD player meters carrying health/hydration/nourishment/energy/morale, campaign resources carrying total plus three faction reputation values, site-tile projections now also carrying quantized device integrity for occupied-entity overlays, and a pending between-phase `SITE_SCENE_READY` auto-ack flag used when smoke hosts transition into `SITE_LOADING` through the renamed host-message boundary.
- `smoke_engine_host.cpp`: Smoke host implementation, including frame-thread ownership of runtime API calls and projection mutation, incoming host-message draining, published snapshot/patch refresh, authoritative site phone home/app-panel state with pending-claim/claimed task counts, active site-modifier snapshot intake plus modifier-only patch publication, site-tile local-wind plus ecology-meter, excavation-depth, device-integrity, and protection-channel projection intake for viewer-reactive plants, terrain shading, excavation marks, and protection heatmaps, smoke-log naming support for sparse plant/device visual upsert-remove messages, host-side conversion of overlay protection channels into final displayed per-tile shelter by combining received protection with the shared floored density-scaled self heat/wind/dust resolved-resistance helper on occupied plant tiles, placement-preview impact-tile intake for hypothetical overlay rendering during build/plant mode, site-protection overlay state projection application, weather-event timeline projection application, HUD meter projection intake including nourishment, site-session cash plus total-and-per-faction reputation projection intake, recent one-shot cue capture for browser audio and reward-claim/campaign-unlock/crafted-output celebration playback, an auto-queued between-phase `SITE_SCENE_READY` host message whenever the smoke runtime enters `SITE_LOADING` so bootstrap resumes without real async resource work, and log-level-aware filtering so debug gameplay logs stay out of activity-only smoke output while remaining available in verbose mode.
- `smoke_host_main.cpp`: Standard smoke host executable entry point, including the optional `--verbose` switch for full engine logging and the shared repo-level `project/` config root passed into gameplay runtime creation.
- `smoke_live_http_server.h`: HTTP server interface for exposing live smoke state.
- `smoke_live_http_server.cpp`: HTTP server implementation for the visual smoke viewer, including the neutral `/site-inventory-slot-tap` endpoint plus passthrough endpoints for gameplay-authored technology node/tier tables and adapter-owned progression metadata consumed by the browser tech-tree overlay, with gameplay facts served from `project/content/` and presentation copy served from `project/adapter_metadata/`.
- `smoke_live_state_json.cpp`: JSON serialization of live smoke/runtime state, including tracked worker-pack visibility, active modifier badge payloads with adapter-owned names and descriptions plus generated fallback tooltip text and timed-buff remaining-game-hour buckets, phone home/app-panel section/count/open snapshot fields plus launcher/app unread-badge booleans, HUD player-meter JSON including nourishment, pending-claim and claimed task list-kind names, site-tile local-wind plus ecology-meter, excavation-depth, device-integrity, and protection fields for plant animation, terrain shading, excavation marks, and protection overlays, placement-preview impact-tile overlay values for hypothetical build/plant hover rendering, site-protection selector/setup and overlay-mode names, weather-event timeline fields, site-session cash plus total-and-per-faction reputation fields serialized as player-facing cash floats, recent one-shot cue payloads for browser audio synthesis plus reward-claim, campaign-unlock, and crafted-output celebration panels, and regional-map tech-tree tab/action/setup names.
- `smoke_log.h`: Smoke logging helper declarations.
- `smoke_log.cpp`: Smoke logging helper implementation.
- `smoke_script_directive.h`: Parsed smoke-script directive/value definitions.
- `smoke_script_runner.h`: Smoke script runner interface.
- `smoke_script_runner.cpp`: Smoke script runner implementation, including tech-tree, phone-panel, site-protection overlay, and end-site-modifier UI action parsing for scripts.
- `visual_smoke_host_main.cpp`: Visual smoke host executable entry point, including `--port`, optional `--verbose` handling, the new `--silent-onboarding` headless path, the shared repo-level `project/` config root passed into gameplay runtime creation, queue-driven host command submission from HTTP callbacks including neutral inventory slot taps, the separate site-control lock, tech-tree plus phone-panel, site-protection overlay, and end-site-modifier UI action parsing for browser-driven clicks, the browser-driven harvest and excavate action parser, and a per-frame timing telemetry stream that reports visual-host versus gameplay-DLL cost to the viewer.
- `visual_smoke_silent_onboarding.h`: Shared declaration for the visual smoke host's silent Site 1 onboarding runner, now also reused as the first registered game-process scenario backend.
- `visual_smoke_silent_onboarding.cpp`: Headless visual-host onboarding controller that drives the full Site 1 tutorial chain from host-side projections without starting the browser HTTP workflow, including item slot taps for delivery-box transfer, craft-context polling, placement-preview confirmation, the new storage-crate deployment step between crafting and the first water purchase, phone purchases, reward claims, end-to-end success/failure exit behavior for silent smoke runs, and the player-facing onboarding labels for the Field Kitchen steps, with the same semantic-player automation now serving the dedicated game-process host too.
- `visual_smoke_live.html`: Browser UI shell and HUD/panel styling for the live visual smoke viewer, including the top-right site phone launcher with unread badge, the simplified four-app phone home launcher with per-app unread dots, the enlarged handset, full-height app panels, inline SVG app and inventory icon surfaces, modifier badge strips plus hover tooltip chrome and timed-buff hour-count styling, section scrolling styles, the id-addressable site-session cash card plus the new bottom-left stacked total/Village/Forestry/University reputation tier bars, the tuned site-vitals palette used during site play, a dedicated nourishment meter-bar color treatment alongside the other player meters, explicit `[hidden]` display suppression for the cash/task/modifier subpanels so the regional-map HUD cannot leak site-only chrome, the persistent fixed-width left-HUD tracked-task stack below the player meter with green completed-state styling plus inline reward-claim buttons for ready tasks, a compact top-right utility row that keeps the phone and audio controls side by side at matching size, the site-active player-pack panel layout that can move independently into the left-side bottom slot while the dedicated tech-tree control anchor occupies the former pack slot on the right, the action-progress card that can float below the worker instead of living in the bottom HUD row on desktop, dynamic viewport-height sizing plus shared responsive layout variables for laptop/browser resizing, an additional short-height compaction pass for smaller laptop screens, the centered site-result modal treatment, a centered celebratory reward-claim overlay with backdrop/burst/sparkle VFX, crafted-output slot pulse styling for opened storage panels, the centered half-screen tech-tree overlay treatment shared by the regional map and active site sessions, the centered site-active protection selector modal with icon buttons for wind/heat/dust overlays, the compact icon-first tech-tree cards with left-side reputation cells, lock overlays, and dedicated hover tooltip chrome, the dedicated adapter-performance chip in the status stack, and the viewer audio enable toggle required for browser-resumed synthesized playback.
- `visual_smoke_live_viewer.js`: Client-side scene, HUD, inventory, storage, phone panel, modifier badge rendering, centered site-result overlay, centered reward-claim celebration rendering, and interaction logic for the live visual smoke viewer, including delivery-box onboarding copy, SVG-backed inventory/context/phone/tech icons, checkerboard plant-action plus harvested-good and excavated-stone inventory metadata, the expanded Village shovel/harvest-food item catalog including the baseline Ephedra Stew plus the Site 1 onboarding phone copy with both the explicit hammer-before-device/tool crafting step and the new deploy-storage-crate beat before the first water purchase, DLL-driven tracked worker-pack, phone-panel unread badges for the top-left launcher plus home-screen apps, active-modifier visibility, the shared visual-viewport sizing helper plus resize-observer-driven renderer fitting used for responsive laptop/browser resizing, and the always-visible left-HUD task stack with shorter summary-first task copy plus numeric progress where accepted tasks stay active, pending-claim tasks remain visible in green until the player claims the reward, those ready HUD cards can now claim directly without reopening the phone while claimed history stays out of the HUD and phone task list, neutral primary inventory slot taps sent to gameplay for use-versus-transfer resolution, the worker-anchored action-progress card positioning for active site actions, the dedicated site-active tech-tree button mount that is separate from the player-pack DOM, the site-vitals resource wrapper now resolving by id or local panel fallback so the left HUD keeps rendering when the markup wrapper changes while regional-map mode still collapses site-only cash/task/modifier chrome and explicitly hides the site-phone launcher there, the new four-bar reputation tier stack derived from total and per-faction reputation ladders, panel exclusivity between inventory, phone, and tech while now allowing protection overlays to stay active during build/plant preview, stabilized context-menu rerender behavior during live site patches with nested submenu panels now aligning to the hovered parent entry rather than resetting to the parent panel top, short-right-click placement cancel copy, short-right-click and `Esc` exit for the protection heatmap mode, RMB harvest and rough-excavate actions, timed-modifier hover tooltip plus on-badge remaining-game-hour countdown and right-click manual end support, dedicated dry-straw `2x2` checkerboard rendering with density-scaled low-profile straw height, merged single-mesh rendering for static procedural plants, camp props, structures, and regional-map marker/tile objects with temporary source geometry now disposed after collapsing so long-running sessions do not leak viewer memory, persistent high-contrast rough/careful/thorough excavation tile plates and center glyph overlays that hide under occupiers, a viewer-side adapter FPS readout that samples browser work plus host/DLL timing telemetry, a 2-second density-change alert plane that flashes red when projected plant density drops or green when it rises except for authored constant-withering plants and ignores overlapping retriggers while the same tile alert is still active, density-only plant-withering updates refreshed in place without forcing a full static site-scene rebuild, DLL-driven phone home/app section switching with back/cart navigation, the simplified four-app root menu with the contractor-facing app relabeled to generic services while contractor offers stay hidden, tappable phone task cards that open a dedicated task-detail page with description/reward breakdown plus accept/claim/back actions, CP-backed phone/hud money rendered as `$xx.xx` cash strings, prototype tech-tree overlay rendering for total-reputation unlockable tiers plus one linear faction-tech card per tier/faction that auto-unlocks from faction reputation, with the viewer now merging gameplay facts from `project/content/` and adapter-owned labels/descriptions from `project/adapter_metadata/` by shared globally unique typed ids so locked entries stay visible with lock overlays and adapter-owned tooltip copy, compact icon-plus-short-name tech cards with hover descriptions, locked-yet-hoverable icon overlays, site-session cash and reputation progress display in the site meter panel with the regional-map chrome intentionally omitting cash, tile-authored regional-map placement with visible map cells instead of curved fallback node layouts, site-session tech-tree toggle support with stabilized overlay persistence across lightweight site patches and reliable primary-press DOM bindings for tech-tree controls, deduplicated site movement-control submissions that keep only the latest pending input instead of POSTing every animation frame, synthesized host-side ambience/music/one-shot playback driven by weather, density, app-state, and recent cue projections, lightweight sparkle/burst regeneration for task-reward celebration cues, simplified channel-separated weather/VFX presentation with the post-process output converted through the renderer color-space path, a single smoothed viewer-side wind cache for intensity and direction shared by overlays, streaks, and plant sway, wind-stream streak continuity preserved across site-scene rebuilds with empty-frame backfill, plant-local-wind refreshes applied in place without rebuilding static geometry, plant vertex deformation whose sway intensity follows projected local wind with a strong-site-wind visual fallback while gust direction follows the cached site-weather direction, corrected east/right-based wind streak orientation to match gameplay `weatherWindDirectionDegrees`, a reduced max device-pixel-ratio cap for the viewer renderer and overlay canvas, plus the continuous desert site terrain with cached placement-only tile overlays, ecology-driven per-tile soil/moisture/salinity shading, strict `0`-to-`100` full-tile wind/heat/dust protection heatmaps over final per-tile shelter values, hypothetical placement-impact overlay values and numeric labels layered on top during hover preview, and the occupied-only `Plant Density / Device Integrity` protection overlay that hides empty tiles, centers one numeric panel on each multi-tile plant or device footprint, reuses the shared screen-space overlay canvas for readable values, dismisses reward-claim celebration on any key press or mouse-button press, pulses crafted outputs when the authoritative storage panel reopens after crafting completes, and now keeps the compact audio-toggle copy short enough to fit the matching pill-sized control while leaving the heavier HUD/task/modifier DOM rerenders on state changes instead of every visual frame.
