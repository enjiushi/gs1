# tests/system/source/

Source-authored system tests and supporting fixtures/runner registration code.

## Usage
- Read this file before scanning the folder in detail.
- If you open a child folder, read its `guideline.md` first when present.
- When files or folders in this directory change, update this file in the same change.

## Contents
- `README.md`: Notes on how source-authored system tests in this folder are discovered.
- `action_ecology_placement_system_tests.cpp`: Cross-system regression tests for actions, ecology, placement, harvest-output insertion plus density setbacks, table-driven harvest insertion with one guaranteed dedicated main item plus independent authored-quantity dry-grass and seed fixed-roll coverage, the additive global-plus-Bureau harvest-bonus progression ladder, excavation start occupancy/inventory validation, depth-specific excavation timing, Village shovel carry bonuses including resolved-modifier-owned Village effect-state consumption, Village careful/thorough unlock flow, rough-depth limits, and deterministic stone-loot outcomes, persistent item-based plant-mode rearming, checkerboard `2x2` plant occupancy, full-`100` initial density for item-based plant placements, ecology-side non-anchor multi-tile planting realignment back to the footprint anchor, previous-frame work-efficiency-driven action timing plus current-local-weather-scaled worker meter costs now deferred until completion including recipe-authored craft timing/cost coverage, multi-tile plant wind-exposure behavior, summed owner-specific weather-contribution-driven terrain updates, highway-target sand-cover handling, and batched ecology output expectations.
- `asset_regression_runners.cpp`: Registers and implements runtime runners for asset-authored system tests, including compatibility parsing for legacy normalized-versus-raw ecology meter inputs, seeded ecology fertility/moisture/burial setup for growth regressions, cash-authored economy expectations normalized into internal cash points for phone scenarios, batched ecology entry-count assertions, and the remaining task-board reset regression flow.
- `campaign_progress_system_tests.cpp`: System tests for campaign progression behavior, including dedicated campaign/site time-system coverage for the 30-real-minute in-game day contract, authored regional-map tile adjacency derivation, all four prototype sites remaining visible from campaign start while only eligible ones unlock for deployment, selected-site support assembly, objective-mode completion/failure messaging including cash-target survival progress/completion, non-decreasing faction-reputation rewards, the shared total-reputation ladder that now advances every `100` reputation across the non-starter plant roster plus ten basic food/drink recipes, the linear `32`-tier-per-faction tech ladder with `200 * tier` faction-reputation requirements and automatic unlocks, faction-granted Village stronger-recipe/Bureau device-processing/University research-recipe availability, the Site 1 onboarding shared-recipe timing regression, and tech-tree open/tab flow from both the regional map and active site sessions.
- `craft_system_tests.cpp`: System tests covering crafting behavior, including worker-pack distance cache membership regressions, hammer-gated device/tool crafting across craft-context, direct craft-commit, and live action-start paths, the shared build/deployment pacing regression that now locks device placement to a `10`-real-second completion window at full worker efficiency, early unlock-gated Chemistry Station kit crafting plus baseline hammer recipe assumptions for the Site 1 storage path, the idle-frame craft-cache refresh guard that use test-injected inventory instead of authored startup loadout assumptions, the regression that preserves nearby craft inputs when storage item entities carry 64-bit flecs ids beyond the old 32-bit cache truncation boundary, and degradable device-storage breakage cleanup on non-fixed-integrity devices while the starter delivery crate remains protected by its fixed-integrity bootstrap contract.
- `device_interaction_system_tests.cpp`: System tests for device interaction flows, including approach-to-start action gating without premature worker meter costs and the hammer-gated workbench craft path that still waits for device range before starting.
- `environment_worker_device_system_tests.cpp`: System tests across environment, worker, and device interactions, including prototype Site 1 bootstrap layout for the harvestable starter Desert Ephedra patches near camp, deterministic coherent site-fertility seeding in the site-run factory, Village pack-slot expansion in the site-run factory, Site 4 cash-target survival objective bootstrap coverage, authored hostile site-one background weather, the simplified baseline-stable Site 1 starter-plant pressure expectations, linear `growthPressure`-driven density-speed coverage, non-growable checkerboard constant-withering coverage including the five-real-minute straw decay regression, owner-specific plant/device weather contribution accumulation, snapped 8-direction wind-shadow coverage for plants, footprint-scaled plant shelter reach plus authored nearby-protection ratio coverage, focus-driven salt-to-salinity support behavior, local-wind and wind-protection behavior on plant and support-only neighbor tiles, the batched-ecology planting-completion straw shelter regression path, the adjacent-multitile-footprint full-shelter regression, the floored reduced-density straw support regression that keeps projected shelter aligned to resolved resistance, derived worker energy-cap/work-efficiency behavior, max-weather-based authored device integrity-loss coverage plus fixed-integrity delivery-crate maintenance regression coverage, explicit modifier-id lookup coverage that rejects missing ids instead of bucket fallback, reputation-scaled technology modifier import coverage on the shared `200 * tier` tech scale, hydration/nourishment-scaled background energy-recovery coverage including the active-action pause, calibrated worker passive weather-decay timing and heat-wind-dust ratio coverage plus resolved Village effect-state weather mitigation, site-time-system-aligned weather timeline interpolation with baseline recovery, onboarding-chain baseline-weather lock coverage until authored onboarding tasks finish, windbreak devices, one-sided highway-protection plus cash-target survival weather waves, and direct test-side checks against the runtime subscription-span interface rather than removed legacy predicate helpers.
- `inventory_economy_task_system_tests.cpp`: System tests spanning inventory, economy, site-start delivery-crate loadout seeding, immediate delivery-crate insertion plus overflow queuing, the seeded Site 1 onboarding task chain with auto-accepted first step plus bundled reward candidates, the explicit hammer-before-storage-crate follow-up step, the new deploy-storage-crate-before-buy-water regression, the buy-food follow-up regression that verifies an actual Field Rations purchase advances the onboarding chain, the stable-starter Ephedra onboarding regression that keeps Site 1 starter density flat while the duration task still advances under hostile ecology pressure, and the shared-recipe progression timing around onboarding reputation awards, plus explicit test-side placeholder task seeding for zero-reward completion, resolved buy/transfer/submit/build targets, item/task valuation tuning coverage including player-meter-derived consumable internal cash points, the baseline `Ephedra Stew` recipe/content contract that keeps the cost at `1` `Water` plus `1` Desert Ephedra Sprig while asserting the cooked meter payout beats the summed ingredient internal cash points, plant-linked derived seed pricing, harvest-conversion tuning, and the plant-CP-times-output-ratio harvest-good pricing model, authored task-template-id uniqueness plus hammer-onboarding-template content validation, the current flat timed-modifier cash-point premium, shared buy/sell multiplier pricing, generated phone buy-stock CP budget refreshes with onboarding lockout, periodic task-board rerolls once onboarding clears, authored phone-stock-driven buy-task candidate selection plus unlocked plant-seed gating, depth-specific excavation timing and stone pricing plus calm-weather sell-EV balance coverage, rep-gated phone listing visibility for progression items instead of seeded direct-purchase unlockables, inventory-confirmed drink/eat completion flow including the shortened authored consume-action start duration, site-session cash spending/reward flow, pending-claim task-state coverage, bundled reward-claim fallback that accepts an unspecified projected reward-candidate id by using the first available draft option, reward-effect ordering, shared `modifier_id`-based timed modifier activation/refresh/expiry behavior, buff-driven action-cost reductions, manual timed-modifier removal, configurable timed-buff-cap enforcement plus cap-bias expansion coverage, split Village timed-buff-slot coverage, shared active site-modifier storage for timed buffs plus permanent run modifiers, duplicate-id overwrite behavior, owner-snapshot-driven task tracking, worker-plus-plant duration-task progress, duration/build-task progress, direct phone buy/sell regressions, and test-side routing that now consults each system's `subscribed_*()` span interface instead of removed legacy predicate helpers.
- `system_test_fixtures.h`: Shared campaign/site fixtures and helpers, including default worker-pack setup with an overridable slot count for focused excavation/inventory scenarios, objective configuration helpers including cash-target survival setup, explicit item-definition constants needed by source-authored tests after header dependency cleanup, test-only `SiteSystemHarness` helpers that replace the removed production `SiteSystemContext` bundle, split-runtime fixture syncing that now preserves the live `SiteWorld` handle across compatibility round-trips, plus shared message-count helpers for batched ecology assertions.
